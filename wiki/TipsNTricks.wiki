#summary Some tips and tricks when using CaLIB

 = Calibration =

  * You can skip all frames in which automatic detection failed and then do only them manually by inputing `find(~active_frames)` when asked the frames to process.